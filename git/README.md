# git

git のグローバル設定。

## ファイル

| ファイル | リンク先 | 内容 |
|---|---|---|
| `.gitconfig` | `~/.gitconfig` | ユーザー情報・エイリアス・デフォルト設定 |
| `.gitignore_global` | `~/.gitignore_global` | 全リポジトリ共通の ignore ルール |

## 反映

`just setup` でシンボリックリンクが作成される。

```sh
just setup
```
